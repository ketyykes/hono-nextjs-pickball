> **這是 RED 階段的承諾文件**：只寫「要先寫哪些測試、斷言什麼」，不寫實作邏輯。
>
> 測試名稱一律採用 delta spec 中「驗收」錨點的**逐字名稱**（繁體中文），使 `/opsx:verify`
> 能以腳本機械核對 `it(...)`／`test(...)` 與 spec 的對應，不靠目視。
>
> 單檔執行指令：
> - unit：`pnpm --filter ./nextjs-pickball test --run lib/matchmaker/history-range.test.ts`
> - e2e：`pnpm --filter ./nextjs-pickball test:e2e --grep "matchmaker-history"`
>
> **`--run` 前不可加 `--`**（見 root `CLAUDE.md`）。

## match-history

### Requirement: 歷史區間切點計算

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 一般情形下四個切點依序為今天、本週一、當月 1 日與上月 1 日 | 一般情形的四個切點（PRD 驗算一） | now = 2026-08-15（週六）→ C0=8/15 00:00、C1=8/10 00:00、C2=8/1 00:00、C3=7/1 00:00 | golden path；PRD 驗算一直接成為測試案例，規格與測試的數字逐字相同 | unit |
| 跨月週時當月切點取本週一而非當月 1 日 | 跨月週時當月切點退回本週一（PRD 驗算二） | now = 2026-08-01（週六，本週一 7/27）→ C0=8/1、C1=7/27、C2=7/27（非 8/1）、C3=7/1 | 核心邊際；`min()` 若漏掉這一層，8/1 當天的區間會重疊，是 PRD 明列的失敗情境 | unit |
| 四個切點單調不遞增 | 四個切點單調不遞增 | 對一組橫跨月初、月中、週一、週日、跨年的 now 逐一計算，皆滿足 C3 <= C2 <= C1 <= C0 | 互斥性的結構前提；此性質一旦破，後面所有區間測試都失去意義 | unit |
| 週起始為週一，週日的本週一為六天前 | 週起始為週一，週日屬於前一週 | now = 2026-08-16（週日）→ C1 = 2026-08-10 00:00，不是 2026-08-17 | 邊界；`getDay()` 以週日為 0，直接拿來用就會把週日算成新一週的開始 | unit |
| 切點為當地時區 00:00 而非 UTC 00:00 | 切點為當地時區 00:00 | now = 當地 2026-08-15 23:30 → C0 的當地 getHours/getMinutes/getSeconds/getMilliseconds 皆為 0，且 C0 等於 new Date(2026, 7, 15) 的時間戳 | 邊界；用 UTC 方法實作時，台北晚間的紀錄會被歸成隔日而落出所有區間 | unit |
| 一月時上月切點落在去年 12 月 1 日 | 上月切點跨年 | now = 2027-01-05 → C3 = 2026-12-01 00:00 | 邊界；月份減一的跨年正規化若手寫容易錯成 2027-00 或 2027-12 | unit |
| 切點依注入的 now 計算，與系統時鐘無關 | 切點依注入的現在計算而非系統時鐘 | 以假時鐘把系統時間設為 2030-03-03 後，傳入 now = 2026-08-15 仍得到 2026-08-15 的切點 | 決定性保證；函式若偷取系統時鐘，上面六個測試會在特定日期才通過 | unit |

### Requirement: 五個區間互斥且完整覆蓋

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 任一時間點恰好落入五個區間中的一個 | 任一時間點恰好落入一個區間 | now = 2026-08-15，對含 1970-01-01 與 2100-01-01 兩個極端值的取樣時間點，五個區間 predicate 中恰有一個成立，且與 rangeOfTime 回傳值一致 | PRD 13.5 的硬性驗收項；互斥與完整覆蓋是本 change 存在的理由 | unit |
| 時間點恰為切點時歸入較新的區間 | 時間點恰為切點時歸入較新的區間 | now = 2026-08-15；t = C0/C1/C2/C3 依序得今日/本週/本月/上月，t = C3 減 1 毫秒得更早 | 邊界；半開區間的左閉右開若寫反，切點當天的紀錄會整批跑到隔壁區間 | unit |
| 晚於現在的時間點仍歸入今日而非落空 | 晚於現在的時間點仍歸入今日 | now = 2026-08-15 20:00，t = 2026-08-15 23:59 → 回傳今日，且不拋出例外 | 邊界；PRD 表格的上界寫「現在」，照字面實作會與同節「完整覆蓋」的硬性要求衝突（design Decision 3） | unit |
| 跨月週時沒有任何時間點落入本月 | 跨月週時本月為空區間 | now = 2026-08-01；7/27～7/31 的取樣點皆回傳本週、7/1～7/26 皆回傳上月，整段 7/1～8/1 無任何點回傳本月 | PRD 13.5 第二條驗收項；空區間必須是設計結果而非例外 | unit |

### Requirement: 歷史紀錄依區間篩選與排序

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 篩選結果依對戰時間由新到舊排序 | 篩選結果依對戰時間由新到舊排序 | 同區間三筆紀錄以 `playedAt` 亂序傳入 → 回傳順序為 `playedAt` 遞減 | golden path；使用情境是「剛打完回頭看」，順序錯了整頁的可讀性就沒了 | unit |
| 篩選不修改輸入的紀錄陣列 | 篩選不修改輸入的紀錄陣列 | 呼叫前後輸入陣列的長度、元素順序與各紀錄內容皆相同，且回傳值與輸入不是同一參照 | 純函式保證；`sort` 原地改動，直接排會汙染呼叫端持有的紀錄清單 | unit |
| 開啟歷史頁預設顯示今日區間 | 開啟歷史頁預設顯示今日 | seed 今日與更早各一筆 → 開頁後今日篩選為選中狀態，列表只含今日那筆 | PRD 13.4 的驗收項；預設值錯會讓使用者一開頁就看到空畫面 | e2e |
| 切換區間後只顯示該區間的紀錄 | 切換區間後只顯示該區間的紀錄 | seed 今日與上月各一筆 → 切到上月後只出現上月那筆，今日那筆消失 | PRD 13.4 的驗收項；篩選的核心互動 | e2e |

### Requirement: 歷史紀錄的顯示欄位

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 雙打紀錄顯示 8.2 全部欄位含雙打組成標示 | 雙打紀錄顯示 8.2 全部欄位 | seed 一筆雙打 `MatchHistoryEntry` → 畫面同時出現 `matchId`、`courtNumber`、`playedAt`、`format`、`doublesComposition`、兩隊球員的 `name`、`scoreA`／`scoreB` 與 `winner` | PRD 8.2 的欄位清單是驗收基準；漏欄位是最常見的呈現缺口 | e2e |
| 單打紀錄不顯示雙打組成標示 | 單打紀錄不顯示雙打組成標示 | seed 一筆單打紀錄 → 該筆不出現任何雙打組成標示文字 | 邊界；單打沒有組成可言，顯示空白或「一般雙打」都是錯誤資訊 | e2e |
| 每位球員同時顯示賽前與賽後分數 | 每位球員同時顯示賽前與賽後分數 | 某球員 `ratingBefore` 4.20、`ratingAfter` 4.35 → 該球員區塊同時出現兩個值 | PRD 13.4 的驗收項；只顯示賽後分數會讓評分變化無從得知 | e2e |

### Requirement: 空區間的友善空狀態

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 跨月週時本月顯示空狀態而非錯誤 | 跨月週時本月顯示空狀態而非錯誤 | 假時鐘固定於 2026-08-01 並 seed 7/27～7/31 的紀錄 → 本月顯示友善空狀態且無錯誤字樣，本週如常列出該批紀錄 | PRD 13.5 第二條驗收項；這是本 change 唯一必須靠假時鐘才能重現的情境 | e2e |
| 沒有任何歷史紀錄時顯示引導空狀態 | 沒有任何歷史紀錄時顯示引導空狀態 | 清空 LocalStorage 後開頁 → 顯示說明「完成對戰後才會有紀錄」的引導空狀態 | 首次使用的第一印象；PRD 11 要求錯誤與空狀態都須為繁體中文且說明下一步 | e2e |

### Requirement: 歷史頁的導覽入口

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 可由對戰頁的連結進入歷史頁 | 可由 matchmaker 區段的連結進入歷史頁 | 於 matchmaker 區段點擊歷史紀錄連結 → 網址為 /matchmaker/history 且歷史頁內容出現 | 沒有入口的頁面等於不存在；本 change 唯一觸碰既有檔案之處，需要測試盯住 | e2e |
| 直接開啟 /matchmaker/history 可載入歷史頁 | 直接開啟網址即可載入歷史頁 | 直接前往該網址 → 頁面載入且五個區間篩選皆可見 | 路由不得相依前一個畫面的記憶體狀態；也是後續所有 e2e 的前置條件 | e2e |

### Requirement: 歷史頁唯讀消費既有紀錄

| Test name | Scenario | Assertion | Why first | Tier |
|-----------|----------|-----------|-----------|------|
| 瀏覽與切換區間後 matchmaker:history:v1 內容不變 | 瀏覽與切換區間不改動持久化資料 | 記下開頁前的原始字串 → 依序切換五個區間 → 再讀出的字串與原始逐字相同 | 唯讀是與 M4／M8 並行的前提；一旦本頁回寫，寫入端的行為就無法單獨驗證 | e2e |
| 紀錄於 hydration 後顯示且無 console error | 紀錄於 hydration 後才出現且無 console error | seed 今日紀錄後開頁 → 紀錄正確顯示，且整個載入過程沒有任何 console error（含 hydration mismatch 警告） | 在 render 期間讀 LocalStorage 或取系統時鐘會產生 SSR/CSR 不一致，這是最容易被忽略的呈現缺陷 | e2e |
