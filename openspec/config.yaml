schema: tdd-subagent-worktree

# context 會注入所有 artifact 的產出指示，輸出語言由此控制（見 schema 檔頭說明）
context: |
  所有 artifact（proposal、specs、design、test-plan、overview、execution-plan、
  tasks、environment）與 change 相關文件一律以繁體中文（台灣用語）撰寫；
  程式碼識別字、指令與技術術語保留英文原文。
